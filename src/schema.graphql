# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

input CreateDocumentInput {
  content: String!
  sourceType: String!
  sourceUrl: String
  title: String!
}

input CreatePromptPresetInput {
  description: String
  name: String!
  params: JSON!
  schemaVersion: Int!
  visibility: PromptVisibility!
}

input CreateSummaryJobInput {
  documentId: String!
  idempotencyKey: String
  model: String!
  paramsSnapshot: JSON!
  schemaVersion: Int!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Document {
  content: String!
  createdAt: DateTime!
  id: ID!
  sourceType: String!
  sourceUrl: String
  title: String!
}

"""
The `JSON` scalar type represents JSON values as specified by [ECMA-404](http://www.ecma-international.org/publications/files/ECMA-ST/ECMA-404.pdf).
"""
scalar JSON

type Mutation {
  createDocument(data: CreateDocumentInput!): Document!
  createPromptPreset(data: CreatePromptPresetInput!): PromptPreset!
  createSummaryJob(data: CreateSummaryJobInput!): SummaryJob!
  deleteDocument(id: ID!): ID!
  deletePromptPresetById(id: String!): ID!
  deleteSummaryJobById(id: String!): String!
  updatePromptPreset(data: UpdatePromptPresetInput!): PromptPreset!
  updateUser(data: UpdateUserInput!, userId: String!): User!
}

type PromptPreset {
  createdAt: DateTime!
  description: String
  id: ID!
  name: String!
  ownerId: String!
  params: JSON!
  schemaVersion: Int!
  updatedAt: DateTime!
  visibility: PromptVisibility!
}

enum PromptVisibility {
  organization
  private
  public
}

type Query {
  document(id: String!): Document
  documents: [Document!]!
  promptPreset(id: String!): PromptPreset!
  promptPresets: [PromptPreset!]!
  summaryJob(id: String!): SummaryJob!
  summaryJobs: [SummaryJob!]!
  user(userId: String!): User
}

type SummaryJob {
  attempts: Int!
  createdAt: DateTime!
  documentId: String!
  error: String
  finishedAt: DateTime
  id: ID!
  idempotencyKey: String
  model: String!
  paramsSnapshot: JSON!
  schemaVersion: Int!
  startedAt: DateTime
  status: SummaryJobStatus!
  userId: String!
}

enum SummaryJobStatus {
  done
  error
  queued
  running
}

input UpdatePromptPresetInput {
  description: String
  id: ID!
  name: String!
  params: JSON!
  schemaVersion: Int!
  visibility: PromptVisibility!
}

input UpdateUserInput {
  email: String
}

type User {
  createdAt: DateTime!
  documents: [Document]
  email: String!
  id: ID!
}